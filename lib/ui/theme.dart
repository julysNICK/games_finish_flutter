import 'package:flutter/material.dart';

class AppTheme {
  static const textDark = Color(0xFF262626);
  static const linearGradient = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Color(0xFFF97794),
      Color(0xFF623AA2),
    ],
    stops: [0.0, 1.0],
  );
}
